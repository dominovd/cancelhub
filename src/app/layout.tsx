// Root layout — minimal shell. Real layout is in /[locale]/layout.tsx
// This file is required by Next.js App Router but [locale] handles everything.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
